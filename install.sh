echo "info:" $(git --version);MYDIR=/home/$(whoami)/antare74;EXECT="/bin/bash ${MYDIR}/src/auto-pull.sh";CURDIR="src";if [ -d ${MYDIR} ];then echo "Already installed in ${MYDIR} ...";else mkdir "${MYDIR}";echo "Installing config files in ${MYDIR} ...";cp -r ${CURDIR} ${MYDIR};cd "${MYDIR}";crontab -l>autopull;echo "*/2 * * * * ${EXECT}">>autopull;crontab autopull;rm -rf autopull;f